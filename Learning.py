# the interpreter will look at the code line by line, therefore the functions must be all defined at the beginning
import coolify as cl

name = input("name ")
name = cl.coolify(name)
print(name)

def __main__():

    myname = input("type your name ")
    myname = coolify(myname)

    print(myname)

